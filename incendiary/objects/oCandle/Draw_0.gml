/// @description Draw Latern

depth = -1 * (y + 10);

var sin_num = (sin(sin_val * 2 * pi) + 1) / 2;

draw_set_alpha(1);

draw_sprite(sCandle, image, x, y);

draw_set_alpha(1);
draw_set_color(c_black);