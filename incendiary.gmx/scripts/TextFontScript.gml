///TextFontScript(x, y, text, font, color, spd);

with(instance_create(argument0, argument1, oText)){
    text = argument2;
    text_font = argument3;
    text_color = argument4;
    spd = argument5;
}
